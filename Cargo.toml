[workspace]
resolver = "2"
members = [
    "types",
    "contracts/mailbox",
    "contracts/merkle-tree-hook",
    "contracts/ism-multisig",
    "contracts/validator-announce",
    "contracts/test-recipient",
    "contracts/test-mock",
    "contracts/protocol-fee",
    "contracts/igp",
    "contracts/warp-drc20",
    "contracts/warp-drc20-collateral",
    "contracts/warp-native",
    "wasm-bindings",
    "data-driver",
    "tests",
    "e2e",
    "dusk-tx",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# Internal
hyperlane-dusk-types = { path = "./types" }

# Dusk
# Using local rusk fork for secp256k1_recover host function
dusk-core = { path = "../../rusk/core" }
dusk-forge = { path = "../forge" }
dusk-data-driver = "0.3"

# Serialization
bytecheck = { version = "0.6.12", default-features = false }
rkyv = { version = "0.7", default-features = false, features = [
    "alloc",
    "size_32",
] }
serde = { version = "1", default-features = false, features = [
    "derive",
    "alloc",
] }
serde_json = { version = "1", default-features = false }

# Testing
dusk-vm = { version = "=1.4.0", default-features = false }
rand = "0.8"
ff = { version = "0.13", default-features = false }
rusk-prover = "1.3"
wasmtime = "25"

# Pin to avoid edition2024 issues
blake2b_simd = "=1.0.3"
blake3 = "=1.5.4"
constant_time_eq = "=0.3.1"
time-core = "=0.1.6"

# Enable overflow checks in release builds for contract safety
[profile.release]
overflow-checks = true
